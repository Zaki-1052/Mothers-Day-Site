# /src/.env
# Purpose: Environment configuration for Mother's Day Carousel frontend

# API Base URL - use empty string for same-origin or specify full URL for different origin
VITE_API_BASE=

# Enable debugging (optional)
VITE_DEBUG=false